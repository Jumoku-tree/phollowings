<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Phollowings</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= stylesheet_pack_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </head>
  <body>
  <header class="d-flex flex-wrap py-3 ps-5 pe-5 bg-white justify-content-between align-items-center">
    <span class="fs-4">
      <%= link_to root_path do %>
        <%= image_tag asset_path('/assets/Logo.png'), height: "60" %>
      <% end %>
    </span>
    <div class="row g-5 w-50 justify-content-end">
      <div class="col-8 d-flex">
        <%= form_with(url: search_works_path, local: true, method: :get, class: "form-control border-0") do |form| %>
          <%= form.text_field :keyword, placeholder: "作品を検索する", class: "form-contorol col-9" %>
          <%= form.submit "検索", class:"col-2" %>
        <% end %>
      </div>
      <div class="col-3">
        <% unless current_page?(new_work_path) %>
        <%= link_to new_work_path do %>
        <button type="button" class="btn btn btn-secondary">投稿する</button>
        <% end %>
        <% end %>
      </div>
    </div>
  </header>
  <main>
    <%= yield %>
  </main>
  </body>
</html>
